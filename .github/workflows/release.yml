name: Release

on:
  pull_request:
  workflow_dispatch:
  push:
    tags: ["v*"]

jobs:
  release:
    permissions:
      contents: write
    uses: source-gamma/.github/.github/workflows/python-release.yml@a3e25899dda04f4f1a573160d1b9d0e673a42421 # v11
